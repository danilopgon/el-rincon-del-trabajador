---
interface Props {
  size?: number;
  color?: string;
}
const { size = 120, color = '#CCA43B' } = Astro.props;
---
<div
  style={`width:${size}px;height:${size}px;background:${color};clip-path:polygon(0 0,100% 100%,0 100%)`}
></div>
